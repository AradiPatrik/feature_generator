package {{ base_package }}.{{ flat module }}.impl.{{ flat first_page }}.screen

import androidx.lifecycle.ViewModel
import androidx.lifecycle.viewModelScope
import {{ base_package }}.{{ flat module }}.impl.{{ flat first_page }}.usecase.DoExample
import kotlinx.coroutines.launch
import javax.inject.Inject

class {{ pascal first_page }}ScreenViewModel @Inject constructor(
    private val doExample: DoExample,
) : ViewModel() {
    fun onClick() = viewModelScope.launch {
        doExample("example")
    }
}