package {{ base_package }}.{{ flat module }}.impl.{{ flat first_page }}.di

import {{ base_package }}.{{ flat module }}.impl.{{ flat first_page }}.usecase.DoExample
import {{ base_package }}.{{ flat module }}.impl.{{ flat first_page }}.usecase.DoExampleUseCase
import {{ base_package }}.navigation.SubfeatureScoped
import dagger.Binds
import dagger.Module

/**
 * Add subfeature-wide available bindings here
 */
@Module
interface {{ pascal first_page }}Module {
    @Binds
    @SubfeatureScoped
    fun bindDoExampleUseCase(impl: DoExampleUseCase): DoExample
}