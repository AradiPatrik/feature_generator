plugins {
    id("com.android.library")
    id("org.jetbrains.kotlin.android")

    `android-config`
}

android {
    namespace = "{{ flat base_package }}.{{ flat module }}.api"
}

dependencies {
    implementation(libs.coroutines)
}
