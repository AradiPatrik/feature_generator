package {{ base_package }}.{{ flat module }}.api

import androidx.navigation.NamedNavArgument
import {{ base_package }}.navigation.EmptyInput
import {{ base_package }}.navigation.FeatureEntry
import {{ base_package }}.navigation.NavigationCommand
import {{ base_package }}.navigation.NavigationCommandProvider
import {{ base_package }}.navigation.createNavDirection
import {{ base_package }}.navigation.NavDirection


/**
 * Define arguments and start destination for the feature.
 */
interface {{ pascal module }}FeatureEntry : FeatureEntry<EmptyInput> {
    companion object :
        NavDirection<EmptyInput> by createNavDirection("{{ kebab module }}")
}
